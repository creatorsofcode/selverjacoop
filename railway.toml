[deploy]
startCommand = "python -m gunicorn app:app --bind 0.0.0.0:${PORT:-10000} --workers 2 --timeout 120 --access-logfile - --error-logfile -"

[variables]
MISE_PYTHON_GITHUB_ATTESTATIONS = "false"
